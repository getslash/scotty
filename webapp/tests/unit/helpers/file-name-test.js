import { fileName } from 'webapp/helpers/file-name';
import { module, test } from 'qunit';

module('Unit | Helper | file name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fileName([42]);
  assert.ok(result);
});
