import {
  newlinesText
} from '../../../helpers/newlines-text';
import { module, test } from 'qunit';

module('NewlinesTextHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = newlinesText(42);
  assert.ok(result);
});
