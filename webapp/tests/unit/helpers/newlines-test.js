import {
  newlines
} from '../../../helpers/newlines';
import { module, test } from 'qunit';

module('NewlinesHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = newlines(42);
  assert.ok(result);
});
