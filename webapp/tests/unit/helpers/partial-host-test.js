import { partialHost } from 'webapp/helpers/partial-host';
import { module, test } from 'qunit';

module('Unit | Helper | partial host');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = partialHost([42]);
  assert.ok(result);
});
