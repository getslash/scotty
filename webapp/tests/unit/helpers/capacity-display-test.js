import {
  capacityDisplay
} from '../../../helpers/capacity-display';
import { module, test } from 'qunit';

module('CapacityDisplayHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = capacityDisplay(42);
  assert.ok(result);
});
