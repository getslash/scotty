SQLALCHEMY_DATABASE_URI: "postgresql://{{app_name}}:{{app_name}}@localhost/{{app_name}}"
STORAGE_PATH: /var/scotty
SENTRY_DSN: "{{sentry_dsn}}"

TRANSPORTER_HOST: "{{transporter_host}}"
VACUUM_THRESHOLD: {{vacuum_threshold}}
PIN_REMIND_THRESHOLD: {{pin_remind_threshold if pin_remind_threshold else "null"}}
SMTP: "{{smtp}}"
BASE_URL: "{{base_url}}"
COMBADGE_CONTACT_TIMEOUT: {{combadge_contact_timeout}}
SHA512SUM: "{{sha512sum if sha512sum else /usr/bin/sha512sum}}"
FREE_SPACE_THRESHOLD: {{free_space_threshold if free_space_threshold else 85}}

DEBUG: {% if install_with_debug is defined and install_with_debug -%}
true
{%- else -%}
false
{%- endif %}
